/**
 * Convenience for creating/sharing key=value regex in case we change it later.
 */
export declare function createKeyValueArgRegex(): RegExp;
/**
 * Convenience for creating/sharing key=value regex in case we change it later.
 * Matches the given key.
 */
export declare function createKeyValueArgRegex(key: string): RegExp;
